grails-runtime-logging
======================

Grails plugin that sllows you to change the logging characteristics (e.g. Level) for common parts of a Grails application at runtime without the need to restart.
